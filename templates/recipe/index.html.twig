{% extends 'base.html.twig' %}


{% block title "Toute les recettes" %}


{% block body %}
	<a href="{{ path('recipe.create') }}">Créer une nouvelle recette</a>
	<table class="table">
		<thead>
			<tr>
				<th>Titre</th>
				<th>Actions</th>
			</tr>

		</thead>
		<tbody>
			{% for recipe in recipes %}
				<tr>
					<td>
						<a href="{{ path('recipe.show', {id: recipe.id, slug:recipe.slug}) }}">{{ recipe.title }}</a>
					</td>
					<td>
						<div class="d-flex gap-2">
						<a class="btn btn-primary btn-sm" href="{{ path('recipe.edit', {id: recipe.id}) }}">Éditer</a>
						<form action="{{ path('recipe.delete', {id: recipe.id}) }}" method="post">
						<input type="hidden" name="_method" value="DELETE">
							<button type="submit" class="btn btn-danger btn-sm">
								Supprimer
							</button>
						</form>
						</div>
					</td>

				</tr>
			{% endfor %}
		</tbody>

	</table>


{% endblock %}
